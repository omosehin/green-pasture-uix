# Flat, real node_modules (no pnpm symlinks) so Next's `output: standalone`
# bundles real folders. Symlinks into .pnpm/ don't survive xcopy + WebDeploy
# to the Windows host and cause: Cannot find module 'next'.
node-linker=hoisted
