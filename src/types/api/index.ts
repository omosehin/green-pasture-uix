/**
 * API Type Definitions
 *
 * This module provides types generated from the backend's OpenAPI/Swagger spec.
 *
 * To regenerate types after backend changes:
 *   1. Start the backend: cd green-pasture-api && pnpm start:dev
 *   2. Run: pnpm generate-types
 *
 * The generated types live in ./generated.ts and are re-exported here
 * with convenient aliases for use throughout the frontend.
 */

// Re-export generated types when available
// After running `pnpm generate-types`, uncomment the line below:
// export type { paths, components, operations } from './generated';
